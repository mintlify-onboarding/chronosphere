
<br />

You must have an API token generated from {props.unrestricted ? "an unrestricted " : "a "}
<em>service account</em> to authenticate with {props.tokenFor ?? "the Chronosphere API"}.

[Find out more about service accounts](/administer/accounts-teams/service-accounts)
and the types Chronosphere supports.

<span>
  To create 

  {props.unrestricted ? "an unrestricted " : "a "}

   service account:
</span>

1.  In the navigation menu select **Settings <span aria-label="and then">></span> Service Accounts**.
2.  In the upper-right of the page, click **Service Account**. An **Add Service Account**
    dialog appears.
3.  In the **Service Account Name** box, enter a descriptive name for the service account.
4.  In the **Service Account Type** section,
    <>{
      props.unrestricted ?
     <> select <strong>Unrestricted.</strong></>
      :
     <> select the option for the type of service account you need.</>
    }</>
5.  Click **Save**.
6.  Copy the resulting API token.

<Warning>
Store your API token in a secure location. If you lose your token, you must create
  a new service account.
</Warning>
